# 2026-03-30 — Workspace layout and transcript performance

## Human update

- User asked to rename all **Baoyu**-branded UI code and routes to **Simplified version** (`/simplified-version`, `simplified-version-*.js`, `.simplified-version-*` CSS). The vendored Cursor skill folder remains [`.cursor/skills/baoyu-youtube-transcript/`](../.cursor/skills/baoyu-youtube-transcript/) to match the upstream package name
- User asked to implement the planned ng.html-style layout: bounded workbench height, transcript scroll inside its panel, compact video meta, and fewer DOM nodes for long transcripts.

## Changes

- **Layout ([`src/ui/styles.js`](../src/ui/styles.js), [`src/ui/page.js`](../src/ui/page.js))**: `workspace-grid` uses a viewport-based height; `left-column` is a flex column with `panel--video` (shrink-wrap) and `panel--transcript` (`flex: 1`, `min-height: 0`). Transcript content scrolls in `#transcript-scroll`. Narrow screens relax fixed height so stacked columns can scroll naturally.
- **Video meta ([`src/ui/client.js`](../src/ui/client.js))**: Replaced duplicate title `notice-card` with a one-line `video-meta-bar` (channel + Open on YouTube).
- **Transcript ([`src/ui/client.js`](../src/ui/client.js))**: For “All cues” with long lists, virtualized rendering (estimated row height + spacers) with scroll listener; cue clicks use delegation on `transcript-scroll`. Auto-follow scrolls the transcript panel for virtual mode before re-render.
- **Profiling**: Use DevTools Network (response size/time) and Performance; optional `?workspacePerf=1` logs `json_parse_ms` and `cue_count` in the console.
- **Tests**: [`src/lib/transcript-virtual-window.js`](../src/lib/transcript-virtual-window.js) + [`tests/transcript-virtual-window.test.js`](../tests/transcript-virtual-window.test.js) lock the virtual window math; [`tests/page.test.js`](../tests/page.test.js) asserts `panel--video` / `panel--transcript` / `#transcript-scroll`.

## Simplified version transcript page `/simplified-version` (2026-03-30)

- **Routes** ([`src/worker.js`](../src/worker.js)): `GET /simplified-version` → [`renderSimplifiedVersionTranscriptPage`](../src/ui/simplified-version-page.js); `GET /assets/simplified-version.js` → [`SIMPLIFIED_VERSION_CLIENT_SOURCE`](../src/ui/simplified-version-client.js).
- **UI**: Single URL input, **Load transcript**, status line, video title strip, scrollable cue list. Loads **`/api/workspace`** then **`/api/transcript?stream=1`**; appends cues as NDJSON chunks arrive.
- **Styles**: `.simplified-version-*` in [`src/ui/styles.js`](../src/ui/styles.js). Skill doc links to `/simplified-version` in [`.cursor/skills/baoyu-youtube-transcript/SKILL.md`](../.cursor/skills/baoyu-youtube-transcript/SKILL.md).

## Transcript NDJSON streaming (2026-03-30)

- **Why it felt slow**: `/api/transcript` still waits on YouTube timedtext + server parse before any bytes; the hard cap “under 5s” depends on network and caption size. The expensive part for the **browser** was one huge `JSON.parse` on the full cue array.
- **Change**: `POST /api/transcript?stream=1` returns **`application/x-ndjson`**: a `head` line (language, source, total), then **one JSON line per 100 cues** (`TRANSCRIPT_NDJSON_CHUNK_SIZE`, [`src/lib/render-model.js`](../src/lib/render-model.js)), then `done`. The client ([`src/ui/client.js`](../src/ui/client.js)) parses **line-by-line**, appends cues, and re-renders on rAF; status shows `{loaded} / {total} cues loaded` while streaming. Non-stream `POST /api/transcript` (no query) is unchanged for small payloads or debugging.
- **Profiling**: `?workspacePerf=1` logs `transcript_first_chunk_ms` and `transcript_stream_total_ms`.
- **Further gains (not implemented)**: cache watch-page fetch between workspace + transcript, stream-parse YouTube json3, or edge cache by `videoId`.

## Split workspace vs transcript API (2026-03-30)

- **`POST /api/workspace`** ([`src/worker.js`](../src/worker.js), [`src/lib/youtube.js`](../src/lib/youtube.js)): Returns video metadata and a **pending** transcript stub (caption language hint, `entries: []`). Small JSON, fast `JSON.parse`.
- **`POST /api/transcript`** (same): Same `{ url }` body; loads timedtext cues via [`fetchTranscriptPayload`](../src/lib/youtube.js). Client merges into `state.workspace.transcript` and clears `pending` ([`src/ui/client.js`](../src/ui/client.js)). **Trade-off:** the worker may fetch the YouTube watch page twice per load (once per endpoint); acceptable for separating payload latency.
- **Model** ([`src/lib/render-model.js`](../src/lib/render-model.js)): `makeWorkspacePayload` / `makeTranscriptApiPayload` and `transcript.pending` flag.

## Auto-follow sync (2026-03-30 follow-up)

- **Clarification**: `ref/ng.html` is a local reference capture only; the app does not fetch it. Slowness on load is dominated by `/api/workspace` network + `JSON.parse` on the transcript payload (see `?workspacePerf=1`), not by batch-loading that file.
- **Change ([`src/ui/client.js`](../src/ui/client.js))**: Cue polling for highlight + auto-follow was 800ms and used `scrollIntoView({ behavior: 'smooth' })`, which kept the list visually behind playback. Polling is now 100ms and auto-follow uses `behavior: 'auto'` so the panel jumps to the active cue immediately.

## Hero wordmark spacing (2026-03-30)

- **Change ([`src/ui/styles.js`](../src/ui/styles.js))**: `.hero-wordmark` `padding-bottom` increased from `20px` to `36px` (narrow breakpoint `28px`) so the decorative wordmark clears the content below with more air.

## Hero sublink alignment (2026-03-30)

- **Change ([`src/ui/styles.js`](../src/ui/styles.js), [`src/ui/page.js`](../src/ui/page.js))**: `Simplified version · captions-only view` sits at the **bottom** of `.hero-title-zone` via flex (`margin-top: auto` on `.hero-topline`) with **`align-self: flex-end`** so its right edge matches the theme/locale cluster above. Removed unused `.hero-brand` wrapper markup and CSS.

## Hero sublink upper-left (2026-03-30 follow-up)

- **Change ([`src/ui/styles.js`](../src/ui/styles.js))**: `.hero-topline` is **`position: absolute`** at **`top`/`left`** matching `.hero-wordmark` insets (`18px` / `24px`, narrow `16px` / `14px`), **`text-align: left`**, with **`max-width`** so it does not run under the theme/locale cluster. Reverted `.hero-title-zone` flex column layout.

## Hero wordmark top flush (2026-03-30)

- **Change ([`src/ui/styles.js`](../src/ui/styles.js))**: `.hero-wordmark` uses **`top: 0`** (narrow **`top: 0`**) and **`bottom` removed** so the decorative line sits on the **top row** of `.hero-title-zone` without extra vertical offset.

## Locale toggle fix (2026-03-30)

- **Cause**: [`applyLocale`](../src/ui/client.js) always set **`refs.heroDescription.textContent`**, but **`#hero-description`** is not present in [`page.js`](../src/ui/page.js), so **`refs.heroDescription`** was **`null`** and **`applyLocale` threw**, breaking the EN/中文 switch after click.
- **Fix**: Only update hero description copy when **`refs.heroDescription`** exists.

## Hide Generation Controls (2026-03-30)

- **Change ([`src/ui/page.js`](../src/ui/page.js))**: First analysis sidebar section (**Generation Controls**) is **`hidden`** with **`id="generation-controls-section"`** so it can be toggled later without losing form fields the client still reads.
- **Todo**: Listed in [UPDATE_LOG/README.md](README.md) under **Current Todo**.

## Hide Detail Pane (2026-03-30)

- **Change ([`src/ui/page.js`](../src/ui/page.js))**: **Detail Pane** sidebar section is **`hidden`** with **`id="detail-pane-section"`**; `#detail-pane` remains in the DOM for [`client.js`](../src/ui/client.js).
- **Todo**: [UPDATE_LOG/README.md](README.md) **Current Todo**.

## Locale toggle follow-up (2026-03-30)

- **CSS**: [`.hero-topline`](../src/ui/styles.js) used a full-width stacking layer (`z-index: 2`) that could intercept pointer events; it now uses **`pointer-events: none`** with **`pointer-events: auto`** on **`.hero-sublink`**, and **`.hero-chrome-cluster`** **`z-index`** is raised to **`10`** so theme/locale buttons stay clickable.
- **JS**: Guard **`refs.appDescription`**, **`refs.localeToggle`**, and **`refs.localeToggleText`** in **`applyLocale`**; only attach locale **`click`** when **`refs.localeToggle`** exists.
- **Markup**: Removed stray **`<div class="hero">`** from [`page.js`](../src/ui/page.js).

## Hero wordmark flow + vertical rhythm (2026-03-30)

- **Why**: Wordmark + `.hero-topline` were **`position: absolute`**, so they did not expand `.hero-title-zone` height; a fixed **`min-height`** let oversized **`clamp()`** text overflow and overlap the control section, while the top looked empty.
- **Change ([`src/ui/styles.js`](../src/ui/styles.js), [`src/ui/page.js`](../src/ui/page.js))**: `.hero-wordmark` is **`position: relative`** in normal flow; **`.hero-title-zone`** uses **`padding: 14px 0 22px`** (narrow **`10px 0 18px`**) instead of a vw-based **`min-height`**. **`.hero-topline`** is **relative** with **`padding-right`** reserved for the chrome cluster. **Font size** uses a gentler **`clamp`** (max ~**`3rem`** desktop, **`2.5rem`** narrow); **`line-height: 1.08`**. Removed stray **`<div class="hero">`**, fixed invalid **`text-align: left top`** and **`padding: -10`**.

## Baoyu speaker transcript + Gemini stream (2026-03-30)

- **What**: Implemented the attached plan: baoyu **`--speakers`** fetch (documented), local **`streamGeminiText`** CLI over bundled **`speaker-transcript.md`**, Worker **`POST /api/speaker-transcript/stream`** (SSE `speaker_chunk`), and docs comparing **`ref/example.txt`** vs verbatim baoyu output.
- **Files**: [`docs/baoyu-speaker-pipeline.md`](../docs/baoyu-speaker-pipeline.md), [`src/lib/speaker-transcript.js`](../src/lib/speaker-transcript.js), [`src/lib/speaker-transcript-instruction.json`](../src/lib/speaker-transcript-instruction.json) (synced from skill prompt), [`scripts/stream-speaker-transcript.mjs`](../scripts/stream-speaker-transcript.mjs), [`scripts/sync-speaker-prompt.mjs`](../scripts/sync-speaker-prompt.mjs), [`scripts/load-dev-vars.mjs`](../scripts/load-dev-vars.mjs), [`src/worker.js`](../src/worker.js), [`tests/speaker-transcript.test.js`](../tests/speaker-transcript.test.js), [`tests/worker.test.js`](../tests/worker.test.js), [`package.json`](../package.json), [`README.md`](../README.md).

## Cloudflare deploy docs, root index stub, @google/genai (2026-03-30)

- **What**: Clarified GitHub → Workers deploy (no production `index.html` required; HTML from [`src/worker.js`](../src/worker.js)); added repo-root [`index.html`](../index.html) as a human-readable note only; [`docs/cloudflare-github-deploy.md`](../docs/cloudflare-github-deploy.md); GitHub Action [`.github/workflows/deploy-worker.yml`](../.github/workflows/deploy-worker.yml); dependency **`@google/genai`** in [`package.json`](../package.json) with Worker still using [`src/lib/gemini.js`](../src/lib/gemini.js) `fetch` until an SDK migration is validated on Workers.

## Local Gemini config file (2026-03-30)

- **What**: Gitignored [`config/gemini.local.json`](../config/gemini.local.json) (user-maintained; template [`config/gemini.local.example.json`](../config/gemini.local.example.json)); [`predev`](../package.json) runs [`scripts/sync-gemini-local-to-dev-vars.mjs`](../scripts/sync-gemini-local-to-dev-vars.mjs) to merge into **`.dev.vars`** for Wrangler; [`resolveGeminiEnv`](../scripts/load-dev-vars.mjs) for Node CLIs; restored placeholder-only [`.dev.vars.example`](../.dev.vars.example) (any real key that was there is a security risk — rotate in AI Studio if it was ever pushed).
- **CLI check**: [`npm run gemini:ping`](../package.json) runs [`scripts/gemini-ping.mjs`](../scripts/gemini-ping.mjs) — same [`pingLlm`](../src/lib/llm.js) call as the Worker — to verify key/model against the live API.

## SiliconFlow local backend (2026-03-30)

- **What**: When **`AI_ENV=local`** and **`SILICONFLOW_API_KEY`** are set (in [`config/gemini.local.json`](../config/gemini.local.json) / `.dev.vars`), the Worker routes LLM calls through [`src/lib/siliconflow.js`](../src/lib/siliconflow.js) (`POST` SiliconFlow [`/v1/messages`](https://api.siliconflow.cn/v1/messages)) via [`src/lib/llm.js`](../src/lib/llm.js) instead of Google Gemini. Template [`config/gemini.local.siliconflow.example.json`](../config/gemini.local.siliconflow.example.json); [`README.md`](../README.md) documents env vars. **Do not commit real API keys**; rotate any key that appeared in chat.

## API JSON vs HTML error handling (2026-03-30)

- **What**: [`parseApiJsonResponse`](../src/ui/client.js) replaces raw `response.json()` on workspace load so HTML bodies (wrong host / SPA fallback) surface [`statusApiHtmlInsteadOfJson`](../src/ui/client.js) instead of a cryptic parse error; [docs/cloudflare-github-deploy.md](../docs/cloudflare-github-deploy.md) troubleshooting section. Parsing prefers **`Content-Type: application/json`**, BOM strip, and leading `{` / `[` before treating `<` as HTML to avoid false positives. Copy clarifies **same host/port as the page** (Wrangler dev) vs **Python proxy-only** tabs and **Cloudflare `/api/*` → Worker**. Extended to **tab**, **person detail**, **transcript translate**, **NDJSON error**, and **summary stream error** responses so remote HTML (e.g. static-only routes) never surfaces `Failed to execute 'json' on 'Response'`.

## Gemini ping on load workspace (2026-03-30)

- **What**: **`POST /api/gemini/ping`** runs a minimal LLM ping via [`pingLlm`](../src/lib/llm.js) (Gemini or SiliconFlow when `AI_ENV=local`). [`loadWorkspace`](../src/ui/client.js) calls it **in parallel** with **`/api/workspace`** and shows status lines for verifying Gemini + metadata, then Gemini OK + transcript loading. Tests: [`tests/gemini.test.js`](../tests/gemini.test.js), [`tests/worker.test.js`](../tests/worker.test.js).

## YouTube watch page 429 retries (2026-03-30)

- **Cause**: YouTube often returns **HTTP 429** for watch-page fetches from shared / edge egress (e.g. Workers), which surfaced as `Failed to fetch the YouTube watch page (429).` on **`POST /api/workspace`**.
- **Change** ([`src/lib/youtube.js`](../src/lib/youtube.js)): [`fetchYouTubeWatchPage`](../src/lib/youtube.js) retries **429** and **503** up to **4** attempts with **`Retry-After`** (when present) or capped exponential backoff; final **429** error message tells users to wait or open the video in a browser once. Tests in [`tests/youtube.test.js`](../tests/youtube.test.js).

## HTML-not-JSON UX (2026-03-30)

- **Issue**: Cloudflare **HTML error pages** (Worker crash, 5xx) were lumped with “wrong Wrangler URL / static hosting,” and the **same long message appeared twice** (status line + analysis panel).
- **Change** ([`src/ui/client.js`](../src/ui/client.js)): [`describeHtmlApiBody`](../src/ui/client.js) picks **`statusApiHtmlCloudflare`** when the body looks like a Cloudflare page; otherwise **`statusApiHtmlInsteadOfJson`**. Workspace load errors use a short **`statusWorkspaceLoadFailed`** in the status bar and the full explanation only in the panel. Docs: [`docs/cloudflare-github-deploy.md`](../docs/cloudflare-github-deploy.md) troubleshooting bullet on edge HTML.

## Single NDJSON workspace stream (2026-03-30)

- **What**: Main load path uses **`POST /api/workspace?stream=1`** only (plus parallel **`/api/gemini/ping`**): one [`fetchWorkspaceData`](../src/lib/youtube.js) on the Worker, NDJSON **`head`** with full [`makeWorkspacePayload`](../src/lib/render-model.js) + **`transcript.expectedTotal`**, then cue **`chunk`** / **`done`** ([`streamWorkspaceNdjson`](../src/worker.js)). Removed the second round trip to **`POST /api/workspace`** JSON + **`POST /api/transcript?stream=1`**. **`POST /api/transcript?stream=1`** still works but now uses **`fetchWorkspaceData`** internally (no double watch-page fetch). [`simplified-version-client.js`](../src/ui/simplified-version-client.js) uses the same stream.

## YouTube Data API captions (2026-03-30)

- **What**: When **`YOUTUBE_KEY`** and **`YOUTUBE_ACCESS_TOKEN`** are set on the Worker, transcript loading uses [`youtube-data-api.js`](../src/lib/youtube-data-api.js): **`videos.list`**, **`captions.list`** (`https://youtube.googleapis.com/youtube/v3/captions?part=snippet&videoId=…&key=…` + `Authorization: Bearer`), pick a track, **`captions.download`** with **`tfmt=vtt`**, parse WebVTT — **no `youtube.com/watch` scrape**. If either key is missing, [`youtube.js`](../src/lib/youtube.js) keeps the legacy watch-page + timedtext path. Docs: [README](../README.md) Environment; [`.dev.vars.example`](../.dev.vars.example); tests [`tests/youtube-data-api.test.js`](../tests/youtube-data-api.test.js).

## YouTube IFrame API postMessage origin (2026-03-30)

- **Cause**: Console showed **`postMessage` … target origin `https://www.youtube.com` does not match recipient … `workers.dev`** — the IFrame API must use the **real parent origin**, not a placeholder.
- **Fix** ([`src/ui/client.js`](../src/ui/client.js)): **`playerVars.origin`** set to **`window.location.origin`** on **`new YT.Player`**. Server metadata [`buildEmbedUrl`](../src/lib/youtube.js) no longer appends **`origin=https://example.com`**; optional second arg adds **`origin`** when known. [`youtube-data-api.js`](../src/lib/youtube-data-api.js) embed URL matches (no bogus origin).

## Gemini API spacing (2026-03-30)

- **What**: [`gemini.js`](../src/lib/gemini.js) serializes **`pingGemini`**, **`generateGeminiJson`**, and **`streamGeminiText`** so each HTTP call starts at least **15s** after the previous start (helps free-tier RPM bursts). [`llm.js`](../src/lib/llm.js) passes Worker **`env`** so **`GEMINI_MIN_REQUEST_INTERVAL_MS`** works from Cloudflare vars as well as **`process.env`** (Node / Wrangler). Override with **`0`** to disable spacing. Vitest sets **`VITEST=true`** so unit tests do not sleep. See [`.dev.vars.example`](../.dev.vars.example).
- **Note**: Google’s **daily** free-tier caps are separate; spacing does not raise the per-day quota.

## Transcript 15s merged rows (2026-03-30)

- **What**: Removed **Time scope / window length / Prev–Next** controls. **Transcript Window** adds **`15 sec blocks`** (default selected): cues whose `startMs` fall in the same **15s** bucket are merged into **one row** with a **single timestamp** (bucket start). Implementation: [`mergeTranscriptEntriesIntoBuckets`](../src/lib/transcript-merge-buckets.js) + [`tests/transcript-merge-buckets.test.js`](../tests/transcript-merge-buckets.test.js); [`src/ui/client.js`](../src/ui/client.js) duplicates merge logic for the bundle, [`transcriptViewUsesVirtualList`](../src/ui/client.js) enables virtualization for **All cues** and **15 sec blocks**. Playback highlight and seek use synthetic ids `tb-{bucketIndex}`. Legacy [`transcript-time-slice.js`](../src/lib/transcript-time-slice.js) tests remain; UI no longer uses time-window filtering.

## Workspace NDJSON “no head” error (2026-03-30)

- **Cause**: **`Workspace stream did not return a valid head payload`** when the NDJSON stream finished without a **`type: head`** line — e.g. **empty body**, **HTML/JSON** instead of **`application/x-ndjson`**, or **wrong host** so `/api/workspace` never hits the Worker.
- **Change** ([`src/ui/client.js`](../src/ui/client.js)): Track **`headReceived`**, strip **BOM** on lines, reject **`response.body` missing**, throw **`statusWorkspaceStreamNoHead`** with troubleshooting copy; **`?workspaceDebug=1`** logs **`content-type`** when no head.

## Minimal core refactor baseline (2026-03-30)

- **Human update**: User requested implementation of the attached minimal-core plan and asked to keep only the initial minimal `tests/youtube.test.js` block when an unexpected appended test section appeared.
- **What changed**:
  - Added runtime deps in [`package.json`](../package.json): **`youtube-transcript`** and **`@google/genai`**
  - Removed stale branch-mismatched tests and rebuilt focused tests:
    - kept/added: [`tests/page.test.js`](../tests/page.test.js), [`tests/locale-utils.test.js`](../tests/locale-utils.test.js), [`tests/youtube.test.js`](../tests/youtube.test.js), [`tests/gemini.test.js`](../tests/gemini.test.js), [`tests/worker.test.js`](../tests/worker.test.js)
  - Added [`src/lib/youtube.js`](../src/lib/youtube.js): video ID extraction + transcript loading
  - Added [`src/lib/gemini.js`](../src/lib/gemini.js): prompt builder + Gemini summary generation via `@google/genai`
  - Updated [`src/worker.js`](../src/worker.js):
    - `POST /api/transcript` validates `{ url }`, extracts video ID, returns transcript payload
    - `POST /api/summary` validates `{ transcript }`, returns `{ html }`
  - Updated [`src/ui/app-client.js`](../src/ui/app-client.js): `Load Workspace` now calls both APIs and renders returned summary HTML into the main analysis panel
  - Trimmed docs/config for current scope:
    - [`README.md`](../README.md) now documents only the minimal core flow
    - [`.dev.vars.example`](../.dev.vars.example) now keeps only `GEMINI_API_KEY` and `GEMINI_MODEL`
- **Verification**:
  - Focused tests pass: `tests/youtube.test.js`, `tests/gemini.test.js`, `tests/worker.test.js`
  - Full test suite pass recorded after refactor
  - Manual HTTP smoke checks were attempted on local Wrangler; API requests timed out because Wrangler reported an internal runtime error in that interactive run

## Client asset cache fix (2026-03-30)

- **Issue**: Browser still showed old `backend not yet implemented` behavior after client updates.
- **Cause**: Worker served `/assets/app.js` and `/assets/styles.css` with `Cache-Control: public, max-age=3600`, so stale client JS could persist for up to one hour.
- **Fix**: Set `STATIC_ASSET_CACHE_CONTROL` to `no-store` in [`src/worker.js`](../src/worker.js) so latest client bundle is always fetched.

## Hide advanced controls + collapsible URL section (2026-03-30)

- **Request**: Hide `Tone`, `Length`, `Section Density`, and `Related Focus`; collapse the URL controls after `Load Workspace` and show a toggle arrow to reopen.
- **Changes**:
  - [`src/ui/page.js`](../src/ui/page.js):
    - Added `#control-section` wrapper id
    - Added `#control-collapse-toggle` arrow button
    - Marked the four advanced control fields as `hidden`
  - [`src/ui/styles.js`](../src/ui/styles.js):
    - Updated `.control-grid` to URL + action layout
    - Added `.control-section.is-collapsed` behavior
    - Added `.control-collapse-toggle` styling
  - [`src/ui/app-client.js`](../src/ui/app-client.js):
    - Added collapse toggle state and click handling
    - Auto-collapses control section after successful workspace load
    - Keeps arrow visible so users can expand controls again

## Live transcript auto-follow with lead time (2026-03-30)

- **Request**: Make transcript truly live and auto-follow playback with a small lead to hide lag.
- **Changes** in [`src/ui/app-client.js`](../src/ui/app-client.js):
  - Switched to YouTube IFrame API player mounting (`YT.Player`) when available, with fallback iframe embed
  - Added playback polling loop (`200ms`) and row syncing by `player.getCurrentTime() + 0.5`
  - Auto-follow now advances transcript highlight and scrolls to the active row slightly early (`0.5s` lookahead)
  - Transcript window changes now re-sync active row immediately
  - Click-to-seek now uses `player.seekTo()` / `playVideo()` when API player is present

## Smartnote default tab + centered content width (2026-03-30)

- **Request**: Add a new left-most default `Smartnote` tab (example-driven style), keep `AI Summary`, and improve body layout so content uses ~80% width and is centered.
- **Changes**:
  - [`src/lib/render-model.js`](../src/lib/render-model.js): Added `smartnote` tab to `TAB_CONFIG` as the first tab
  - [`src/ui/page.js`](../src/ui/page.js):
    - Default active tab switched to `Smartnote`
    - Workspace subtitle and empty-state copy updated for smartnote-first flow
  - [`src/lib/gemini.js`](../src/lib/gemini.js):
    - Added `buildSmartnotePrompt(...)`
    - Added `generateSmartnote(...)`
    - Refactored generation into shared helper
  - [`src/worker.js`](../src/worker.js): Added `POST /api/smartnote` route with transcript validation
  - [`src/ui/app-client.js`](../src/ui/app-client.js):
    - Added tab state + switching (`Smartnote`, `AI Summary`, fallback notice for deferred tabs)
    - On load, generates both smartnote and summary in parallel
    - Renders smartnote by default
  - [`src/ui/styles.js`](../src/ui/styles.js):
    - `.summary-frame` now renders centered at `80%` width on desktop
    - Mobile override keeps `100%` width
    - Added minor smartnote heading accent style
- **Tests**:
  - Updated: [`tests/gemini.test.js`](../tests/gemini.test.js), [`tests/worker.test.js`](../tests/worker.test.js), [`tests/page.test.js`](../tests/page.test.js)
  - Full suite passes (`16/16`)

## Control collapse behavior + remove regenerate (2026-03-30)

- **Request**: Keep only `Load Workspace`, and make the URL/action section fold immediately into a downward-arrow state after click.
- **Changes**:
  - [`src/ui/page.js`](../src/ui/page.js): Removed `Regenerate` button from `.action-stack`
  - [`src/ui/app-client.js`](../src/ui/app-client.js):
    - Removed `regenerate-summary` references
    - Set `hasLoadedWorkspace = true` and collapse controls at the start of load handling (immediately after click validation), not only after generation finishes
    - Arrow toggle continues to reopen/hide the section on demand

## Client source refactor for debugging + safeVideoId fix (2026-03-30)

- **Request**: `safeVideoId` runtime error in client and easier debugging with syntax highlighting instead of giant raw template text.
- **Changes**:
  - Added [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js) with normal JavaScript function `bootstrapAppClient()` (fully syntax-highlighted)
  - Simplified [`src/ui/app-client.js`](../src/ui/app-client.js) to:
    - import `bootstrapAppClient`
    - export `CLIENT_APP_SOURCE` from `bootstrapAppClient.toString()`
  - Fixed runtime bug in `renderLiveVideo(...)` by defining `safeVideoId` before constructing the `Open on YouTube` link.

## Browser runtime helper fix (`__name`) (2026-03-30)

- **Issue**: Browser threw `Uncaught ReferenceError: __name is not defined` from generated `/assets/app.js` after workspace load.
- **Cause**: `bootstrapAppClient.toString()` contained bundler-injected `__name(...)` calls, but that helper was not present in the emitted browser source string.
- **Fix**: Prepended a no-op shim in [`src/ui/app-client.js`](../src/ui/app-client.js):
  - `const __name = (target) => target;`
  - then invoke serialized bootstrap function.

## Condensed centered URL controls (2026-03-30)

- **Request**: Make `control-grid` more condensed; keep YouTube URL centered and occupying ~40% width.
- **Changes**:
  - [`src/ui/page.js`](../src/ui/page.js): Added `control-field--video-url` class to the URL field
  - [`src/ui/styles.js`](../src/ui/styles.js):
    - Switched `.control-grid` to centered flex layout
    - Added `.control-field--video-url` sizing (`40%` width on desktop)
    - Added responsive fallback to full-width on small screens

## Transcript captcha/rate-limit error handling (2026-03-30)

- **Human update**: User reported `YoutubeTranscript` captcha/rate-limit failures (`too many requests from this IP`) when loading transcripts
- **Change**:
  - [`src/lib/youtube.js`](../src/lib/youtube.js): Added [`mapTranscriptFetchError`](../src/lib/youtube.js) to classify upstream transcript failures into HTTP-aware errors:
    - captcha/rate-limit → **429** with actionable retry guidance
    - transcript unavailable / disabled → **404**
    - video unavailable / restricted → **404**
    - invalid video id → **400**
    - unknown upstream failures → **502**
  - [`src/worker.js`](../src/worker.js): Added [`resolveHttpStatus`](../src/worker.js) and preserved error status from thrown errors instead of always returning 500
  - [`tests/youtube.test.js`](../tests/youtube.test.js): Added coverage for captcha/rate-limit error mapping
  - [`tests/worker.test.js`](../tests/worker.test.js): Added coverage for explicit status passthrough helper
- **Verification**: `npm test -- tests/youtube.test.js tests/worker.test.js` (pass)

## Captcha recovery UI + local yt-dlp fallback (2026-03-30)

- **Human update**: User requested a true workaround when YouTube captcha/rate-limit blocks transcript loading: guided frontend recovery + alternate endpoint path
- **Change**:
  - [`src/lib/youtube.js`](../src/lib/youtube.js): `fetchTranscript(videoId, { env, videoUrl })` now attempts local fallback on `youtube_captcha_required`; captcha error carries structured `code` + `data.recovery.openUrl`
  - Added [`src/lib/local-transcript-fallback.js`](../src/lib/local-transcript-fallback.js): Worker-side client for `LOCAL_TRANSCRIPT_FALLBACK_URL` endpoint (`POST /transcript`)
  - [`src/worker.js`](../src/worker.js): transcript route passes `env` + `videoUrl`; error responses now include structured `{ error, code, data }` via [`resolveApiErrorPayload`](../src/worker.js)
  - [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js):
    - API errors preserve `status/code/data`
    - captcha errors render recovery notice card with **Open YouTube Check** + **Retry Load Workspace**
  - Added local fallback service script [`scripts/local-transcript-fallback-server.mjs`](../scripts/local-transcript-fallback-server.mjs) (Node + `yt-dlp`, `/health`, `POST /transcript`)
  - Updated config/docs: [`package.json`](../package.json) (`dev:fallback`), [`.dev.vars.example`](../.dev.vars.example) (`LOCAL_TRANSCRIPT_FALLBACK_URL`), [`README.md`](../README.md) workaround notes and `YOUTUBE_KEY` limitation

## Captcha open-link behavior tweak (2026-03-30)

- **Human update**: User reported captcha recovery button opening the exact entered watch URL; requested a fix
- **Change**:
  - [`src/lib/youtube.js`](../src/lib/youtube.js): `recovery.openUrl` for captcha now points to generic **`https://www.youtube.com/`** instead of per-video watch URL
  - [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js): recovery action now opens YouTube verification URL, with popup-block fallback to same-tab navigation, and button copy updated to **Open YouTube Verification**
  - [`tests/youtube.test.js`](../tests/youtube.test.js): captcha mapping assertion now checks exact generic URL
- **Verification**:
  - `npm test -- tests/youtube.test.js`
  - `npm test -- tests/worker.test.js`

## Captcha recovery explicit link (2026-03-30)

- **Human update**: User requested an explicit link for `Open YouTube Verification`
- **Change** ([`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js)): captcha recovery notice now includes a visible anchor link (`Open YouTube Verification Link`) to the same verification URL, alongside the button actions
- **Verification**: `npm test`

## API key captions.list implementation (2026-03-30)

- **Human update**: User requested implementing the reference API-key function from [`ref/api.js`](../ref/api.js) into actual app code
- **Change**:
  - [`src/lib/youtube.js`](../src/lib/youtube.js): added [`fetchTranscriptViaApiKey`](../src/lib/youtube.js) that calls YouTube Data API `captions.list` with `YOUTUBE_KEY`, then attempts public `timedtext` retrieval (`fmt=json3`) for discovered language
  - `fetchTranscript(...)` now tries API-key path first when `YOUTUBE_KEY` exists, then falls back to existing `youtube-transcript` and local fallback behavior
  - Added internal helpers for Google API error extraction and json3 parsing
  - [`tests/youtube.test.js`](../tests/youtube.test.js): new unit test covering API-key transcript path with mocked network calls
  - Updated env/docs: [`.dev.vars.example`](../.dev.vars.example) includes `YOUTUBE_KEY`; [`README.md`](../README.md) documents new attempt order and limitations
- **Verification**:
  - `npm test -- tests/youtube.test.js`
  - `npm test`

## OAuth best-practice path (`YOUTUBE_CLIENT_ID`) (2026-03-30)

- **Human update**: User confirmed OAuth client id already exists in env as `YOUTUBE_CLIENT_ID` and requested alignment with `ref/api.js` best-practice flow
- **Change**:
  - [`src/ui/page.js`](../src/ui/page.js): added hidden `Authorize YouTube` button and Google Identity Services script include
  - [`src/worker.js`](../src/worker.js): new `GET /api/config` exposing public `youtubeClientId`; transcript route now accepts optional body field `oauthAccessToken`
  - [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js):
    - fetches `/api/config`
    - initializes GIS token client when `youtubeClientId` is available
    - supports interactive OAuth token grant via `Authorize YouTube`
    - sends `oauthAccessToken` with `/api/transcript` when token is valid
  - [`src/lib/youtube.js`](../src/lib/youtube.js):
    - added [`fetchTranscriptViaOAuth`](../src/lib/youtube.js)
    - unified Google captions path for OAuth/API-key auth and timedtext extraction
    - transcript strategy now tries OAuth token first (if provided), then API key, then existing fallback chain
  - Tests:
    - [`tests/youtube.test.js`](../tests/youtube.test.js): new OAuth bearer-header coverage
    - [`tests/worker.test.js`](../tests/worker.test.js): `/api/config` route coverage
  - Docs/env:
    - [`.dev.vars.example`](../.dev.vars.example): add `YOUTUBE_CLIENT_ID`
    - [`README.md`](../README.md): document OAuth+API-key attempt order
- **Verification**:
  - `npm test -- tests/youtube.test.js tests/worker.test.js tests/page.test.js`
  - `npm test`

## Transcript source label in status line (2026-03-30)

- **Human update**: User requested showing which transcript path is actually used
- **Change**:
  - [`src/lib/youtube.js`](../src/lib/youtube.js): default `youtube-transcript` success payload now includes `source: "youtube_transcript_library"`
  - [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js): status line now reports source after transcript load (OAuth / API key / youtube-transcript / local yt-dlp fallback)
- **Verification**: `npm test`

## Browser OAuth popup transcript return (2026-03-30)

- **Human update**: User pointed out opening `youtube.com` cannot validate Worker egress IP; requested a popup flow that can truly pass local-network validation
- **Change**:
  - Added [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js) and Worker route `GET /popup/youtube-transcript-auth`
  - Popup flow:
    - reads `videoUrl` from query
    - runs Google OAuth (`YOUTUBE_CLIENT_ID` via `/api/config`)
    - requests captions list, then browser-side timedtext
    - parses transcript and sends payload back via `window.opener.postMessage(...)`
  - [`src/ui/app-client-runtime.js`](../src/ui/app-client-runtime.js):
    - captcha recovery action now opens popup route (not plain youtube.com)
    - listens for popup message and hydrates transcript list/status directly
    - recovery card copy/link/button updated to browser OAuth popup wording
  - [`src/worker.js`](../src/worker.js): serves popup page route
- **Verification**:
  - `npm test -- tests/worker.test.js tests/page.test.js`
  - `npm test`

## Popup timedtext parsing fix for auto-generated captions (2026-03-31)

- **Human update**: After OAuth succeeds, popup showed `No transcript entries parsed from timedtext` for videos that display auto-generated subtitles
- **Change** ([`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js)):
  - added json3 parser in popup
  - timedtext fetch now tries `fmt=json3` first and falls back to `fmt=vtt`
  - when track kind is auto-generated (`ASR`), includes `kind=asr` query param for both formats
  - upgraded failure message to show attempted modes for easier diagnostics
- **Verification**:
  - `npm test -- tests/worker.test.js`
  - `npm test`

## OAuth popup page split for syntax support (2026-03-31)

- **Human update**: User requested splitting `oauth-popup-page.js` so syntax highlighting/editing works better
- **Change**:
  - Added [`src/ui/oauth-popup-runtime.js`](../src/ui/oauth-popup-runtime.js) with executable popup logic (`bootstrapOAuthPopup`)
  - Simplified [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js) to HTML shell + runtime injection using `bootstrapOAuthPopup.toString()`
  - Behavior unchanged; this is a maintainability/readability refactor for popup code editing
- **Verification**:
  - `npm test -- tests/worker.test.js`
  - `npm test`

## Popup debug panel for transcript fetch layers (2026-03-31)

- **Human update**: User requested popup debug display showing picked language, track kind, and json3/vtt response lengths
- **Change**:
  - [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js): added `#debug` panel
  - [`src/ui/oauth-popup-runtime.js`](../src/ui/oauth-popup-runtime.js): added live debug state rendering:
    - `picked language`
    - `trackKind`
    - `json3 status` / `json3 length`
    - `vtt status` / `vtt length`
    - selected format and parsed entry count
    - terminal error message
- **Verification**:
  - `npm test -- tests/worker.test.js`
  - `npm test`

## Move popup raw markup out of page renderer (2026-03-31)

- **Human update**: User requested removing raw HTML from [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js) into a separate file for better editing/highlighting flow
- **Change**:
  - Added [`src/ui/oauth-popup-markup.js`](../src/ui/oauth-popup-markup.js) containing popup HTML markup template
  - [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js) now only injects serialized runtime into template placeholder
- **Verification**:
  - `npm test -- tests/worker.test.js`
  - `npm test`

## Split large UI HTML into `.html` sources (2026-03-31)

- **Human update**: User requested moving large HTML blocks in `src/ui` into standalone `.html` files for syntax highlighting
- **Change**:
  - Added HTML source templates:
    - [`src/ui/templates/app-page.html`](../src/ui/templates/app-page.html)
    - [`src/ui/templates/simplified-version-page.html`](../src/ui/templates/simplified-version-page.html)
    - [`src/ui/templates/oauth-popup-markup.html`](../src/ui/templates/oauth-popup-markup.html)
  - Added generator script [`scripts/sync-ui-html-templates.mjs`](../scripts/sync-ui-html-templates.mjs) and npm script `sync-ui-html`
  - Updated `predev` to sync templates automatically
  - Generated modules:
    - [`src/ui/app-page-template.js`](../src/ui/app-page-template.js)
    - [`src/ui/simplified-version-page-template.js`](../src/ui/simplified-version-page-template.js)
    - [`src/ui/oauth-popup-template.js`](../src/ui/oauth-popup-template.js)
  - Refactored renderers to use generated templates with token replacement:
    - [`src/ui/page.js`](../src/ui/page.js)
    - [`src/ui/simplified-version-page.js`](../src/ui/simplified-version-page.js)
    - [`src/ui/oauth-popup-page.js`](../src/ui/oauth-popup-page.js)
  - Removed obsolete [`src/ui/oauth-popup-markup.js`](../src/ui/oauth-popup-markup.js)
- **Verification**:
  - `npm run sync-ui-html`
  - `npm test`

## Popup transcript fetch fallback to captions.download (2026-03-31)

- **Human update**: User still hit `No transcript entries parsed from timedtext`
- **Change** ([`src/ui/oauth-popup-runtime.js`](../src/ui/oauth-popup-runtime.js)):
  - Added OAuth `captions.download` (by `captionId`, `tfmt=vtt`) as first data path in popup
  - Keeps existing timedtext `json3 -> vtt` as fallback paths
  - Added smarter track selection (`standard` track first, then ASR)
  - Expanded debug panel with:
    - `captionId`
    - `captions.download status/length`
    - existing timedtext metrics
- **Verification**:
  - `npm test -- tests/worker.test.js`
  - `npm test`

## Runtime chain adds youtube-caption-extractor (2026-03-31)

- **Human update**: User requested implementing the broader plan directly into Worker runtime logic
- **Change**:
  - Added dependency `youtube-caption-extractor`
  - [`src/lib/youtube.js`](../src/lib/youtube.js):
    - Added [`fetchTranscriptViaCaptionExtractor`](../src/lib/youtube.js)
    - Added robust extractor-entry normalization for varying field shapes (`start/startMs`, `dur/durationMs`)
    - Updated `fetchTranscript(...)` strategy order:
      1. OAuth captions path
      2. API key captions path
      3. `youtube-caption-extractor`
      4. `youtube-transcript`
      5. local fallback for captcha blocks
  - [`tests/youtube.test.js`](../tests/youtube.test.js): added coverage for caption-extractor normalization path
  - [`README.md`](../README.md): updated fallback order docs
- **Verification**:
  - `npm test -- tests/youtube.test.js`
  - `npm test`
