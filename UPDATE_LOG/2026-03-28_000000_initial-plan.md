# Update Log - 2026-03-28 00:00:00

- **Author**: LLM
- **Summary**: Initial plan — set up repo-local Cursor skills, project-level rules, and UPDATE_LOG system

---

## Plan: Setup Repo-Local Skills, Project Rules, and Update Logging

### Current State

- **Repo**: Freshly initialized Git repo with only `README.md`, `LICENSE`, `.gitattributes` — no code yet
- **Built-in skills**: Only 5 built-in meta-skills in `~/.cursor/skills-cursor/` (create-rule, create-skill, etc.) — no coding skills
- **Project skills**: `.cursor/skills/` does not exist yet
- **Project rules**: No `.cursor/` directory in this repo
- **Settings**: Minimal global `settings.json`

---

### Part 1: Project Skills (`.cursor/skills/`)

Create reusable repo-local skills that apply only within this repository.

#### 1a. Node.js Coding Skill

**Path**: `.cursor/skills/nodejs-coding/SKILL.md`

- Node.js best practices, project structure conventions
- npm/package.json standards, dependency management
- ES modules vs CommonJS guidance
- Error handling patterns (async/await, streams)
- Security basics (input validation, env vars, no secrets in code)
- Performance patterns (event loop awareness, clustering)
- Triggers when working with `.js`, `.mjs`, `.cjs`, `package.json`, or Node.js projects

#### 1b. Essential Coding Practices Skill

**Path**: `.cursor/skills/coding-essentials/SKILL.md`

- Language-agnostic best practices that apply to every repo
- Clean code principles, SOLID, DRY, KISS
- Git workflow (meaningful commits, branch naming)
- Code review checklist
- Documentation standards
- Error handling philosophy
- Testing philosophy (unit, integration, e2e)
- Triggers broadly when writing or reviewing code

---

### Part 2: Project-Level Cursor Rules (`.cursor/rules/`)

Create `.mdc` rule files in the repo's `.cursor/rules/` directory.

#### 2a. Project Bootstrap Rule (alwaysApply)

**Path**: `.cursor/rules/project-bootstrap.mdc`

- **alwaysApply: true** — LLM reads this first every session
- Check for and create structured project files if missing (`package.json`, `tsconfig.json`, `src/`, `tests/`, `.gitignore`, `README.md`, etc.)
- Follow Google JavaScript/TypeScript Style Guide conventions
- Enforce Test-Driven Development: write test first, then implement, then refactor
- Auto-research: before implementing, search for best practices; after each iteration, reflect and log learnings

#### 2b. Update Logging Rule (alwaysApply)

**Path**: `.cursor/rules/update-logging.mdc`

- **alwaysApply: true**
- At the end of each session/task, append to `UPDATE_LOG/YYYY-MM-DD_summary.md`
- Template: timestamp, author (LLM or Human), summary of changes, files modified
- Sort convention: one file per day with a summary in the filename, and rename it if the day's scope changes
- Auto-generate `UPDATE_LOG/README.md` as an index that links to all log entries

#### 2c. TDD Workflow Rule

**Path**: `.cursor/rules/tdd-workflow.mdc`

- **globs**: `**/*.test.{js,ts}`, `**/*.spec.{js,ts}`, `src/**/*.{js,ts}`
- Red-Green-Refactor cycle
- Write failing test first, implement minimum code to pass, then refactor
- Every feature/bugfix must have a corresponding test

#### 2d. Google Style Rule

**Path**: `.cursor/rules/google-style.mdc`

- **globs**: `**/*.{js,ts,jsx,tsx}`
- Google JavaScript Style Guide key conventions
- Naming: camelCase for vars/functions, PascalCase for classes/types, UPPER_SNAKE for constants
- JSDoc for public APIs
- Prefer `const` over `let`, never `var`
- 2-space indentation, semicolons required, single quotes

---

### Part 3: UPDATE_LOG System

#### 3a. Directory and Index

- `UPDATE_LOG/` folder at repo root
- `UPDATE_LOG/README.md` — index with links to all entries, sorted newest day first

#### 3b. Log Entry Template

Each daily file: `UPDATE_LOG/YYYY-MM-DD_summary.md`

```
## HH:MM:SS

- **Author**: LLM / Human
- **Summary**: Brief description of changes
- **Files Changed**:
  - `path/to/file` - what changed
- **Dialog Summary** (if LLM):
  - User asked: ...
  - LLM Updated: ...
```

---

### File Tree After Implementation

```
youtube-transcript-to-html/
├── .cursor/
│   └── rules/
│       ├── project-bootstrap.mdc
│       ├── update-logging.mdc
│       ├── tdd-workflow.mdc
│       └── google-style.mdc
├── UPDATE_LOG/
│   ├── README.md
│   ├── 2026-03-28_summary.md
│   └── 2026-03-28_000000_initial-plan.md   ← special initial log
├── .gitattributes
├── LICENSE
├── README.md
└── .cursor/
    └── skills/
        ├── nodejs-coding/
        │   └── SKILL.md
        └── coding-essentials/
            └── SKILL.md
```

---

### Todos

| # | Task | Status |
|---|------|--------|
| 1 | Create `.cursor/skills/nodejs-coding/SKILL.md` | pending |
| 2 | Create `.cursor/skills/coding-essentials/SKILL.md` | pending |
| 3 | Create `.cursor/rules/project-bootstrap.mdc` | pending |
| 4 | Create `.cursor/rules/update-logging.mdc` | pending |
| 5 | Create `.cursor/rules/tdd-workflow.mdc` | pending |
| 6 | Create `.cursor/rules/google-style.mdc` | pending |
| 7 | Create `UPDATE_LOG/README.md` index | pending |
| 8 | Stage and commit all new files | pending |
