{
  "name": "youtube-transcript-to-html",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker — YouTube transcript to AI notes workspace.",
  "scripts": {
    "predev": "node scripts/sync-gemini-local-to-dev-vars.mjs",
    "sync-gemini": "node scripts/sync-gemini-local-to-dev-vars.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.39.0"
  },
  "dependencies": {
    "@google/genai": "^1.47.0",
    "youtube-transcript": "^1.3.0"
  }
}
